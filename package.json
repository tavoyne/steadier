{
  "description": "Steadier is an opinionated code quality checker.",
  "devDependencies": {
    "@rushstack/eslint-patch": "^1.1.0",
    "@steadier/eslint-config": "workspace:^",
    "eslint": "^8.6.0",
    "prettier": "^2.5.1",
    "typescript": "^4.5.4"
  },
  "engines": {
    "yarn": ">=1.4.0"
  },
  "packageManager": "yarn@3.1.1",
  "private": true,
  "scripts": {
    "lint": "eslint && prettier . --check",
    "test": "yarn workspaces foreach run test"
  },
  "workspaces": [
    "packages/*"
  ]
}
