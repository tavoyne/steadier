{
  "devDependencies": {
    "@rushstack/eslint-patch": "^1.1.0",
    "eslint": "^8.5.0",
    "eslint-config-convergence-base": "workspace:^",
    "eslint-config-convergence-json": "workspace:^",
    "eslint-config-convergence-node": "workspace:^",
    "eslint-config-convergence-yaml": "workspace:^",
    "prettier": "^2.5.1",
    "typescript": "^4.5.4"
  },
  "engines": {
    "yarn": ">=1.4.0"
  },
  "packageManager": "yarn@3.1.1",
  "private": true,
  "scripts": {
    "lint": "eslint && prettier . --check",
    "test": "yarn workspaces foreach run test"
  },
  "workspaces": [
    "packages/*"
  ]
}
